module top_level (
   input             CLOCK_50,					// DE2-115's 50MHz clock signal
   input  				GPIO[3],                // TX
	output				GPIO[5]						// RX
	); 
	


   motor_drive			motor_drive_u0
			(.clk(CLOCK_50),
			.left_button_edge(left_button_edge),
			.right_button_edge(right_button_edge),
			.ensure_button_edge(ensure_button_edge),
			.image_signal(image),
			.Reset(Reset),
         .convolution_signal(convolution_signal));