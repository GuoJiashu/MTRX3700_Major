module top_level(
	input clk_50,
	inout	[5:0] GPIO,
	
	output [17:0] LEDR
);

logic reset, ready=1;



arduino_uart_buffer (
	.clk_50(clk_50),
	.reset(reset),
	.arduino_input(GPIO[5]),
	.ready(ready),
	.valid(),
	.arduino_command(LEDR[7:0])
);